[project]
name = "poople-engine"
version = "0.1.0"
description = "An engine to play the word ladder game poople optimally."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "pytest>=9.1.1",
]
